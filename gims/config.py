"""
Спільні налаштування проєкту: шляхи, вимірювальні канали, параметри дослідів.

Усе, що може знадобитися змінювати (горизонти прогнозу, частки вибірок, зерно
генератора випадкових чисел), зібрано тут, щоб не правити це по всьому коду.
"""
from __future__ import annotations

from pathlib import Path

# --------------------------------------------------------------------------
# Шляхи
# --------------------------------------------------------------------------
ROOT = Path(__file__).resolve().parent.parent

DATA_RAW = ROOT / "data" / "raw"              # сюди завантажується архів як є
DATA_PROCESSED = ROOT / "data" / "processed"  # сюди пишуться готові таблиці
RESULTS = ROOT / "results"
FIGURES = RESULTS / "figures"                 # рисунки
TABLES = RESULTS / "tables"                   # таблиці (csv)

for _p in (DATA_RAW, DATA_PROCESSED, RESULTS, FIGURES, TABLES):
    _p.mkdir(parents=True, exist_ok=True)

# --------------------------------------------------------------------------
# Набір даних Autonomous Greenhouse Challenge, 2-ге видання (Wageningen UR).
# Ліцензія CC0 (public domain): використовується вільно, потрібне лише
# посилання на джерело.
# --------------------------------------------------------------------------
AGC_DOI = "10.4121/uuid:88d22c60-21b3-4ea8-90db-20249a5be2a7"
AGC_API = "https://data.4tu.nl/v2/articles/12764777"
AGC_ARCHIVE = DATA_RAW / "AutonomousGreenhouseChallenge_edition2.7z"
AGC_EXTRACT_DIR = DATA_RAW / "agc2"

# --------------------------------------------------------------------------
# Вимірювальні канали приладу.
# Ключ — внутрішнє ім'я каналу, використовується в усьому коді.
# --------------------------------------------------------------------------
CHANNELS = {
    "T_air": dict(sensor="DHT22", unit="°C", descr="температура повітря"),
    "RH_air": dict(sensor="DHT22", unit="%RH", descr="відносна вологість повітря"),
    "CO2_air": dict(sensor="SGP30", unit="ppm", descr="еквівалентна концентрація CO2"),
    "LUX": dict(sensor="BH1750", unit="лк", descr="освітленість"),
    "SOIL": dict(sensor="CapSoilADC", unit="%", descr="вологість субстрату"),
}

# Величини, які прогнозуємо (саме ними реально керують у теплиці)
TARGETS = ["T_air", "RH_air", "CO2_air"]

# --------------------------------------------------------------------------
# Параметри дослідів
# --------------------------------------------------------------------------
SAMPLE_PERIOD_S = 300          # крок вихідних даних AGC — 5 хвилин
HORIZONS_MIN = [15, 30, 60]    # горизонти прогнозу, хвилини
HORIZONS = [h * 60 // SAMPLE_PERIOD_S for h in HORIZONS_MIN]  # у відліках: 3, 6, 12

LOOKBACK = 24                  # довжина вхідного вікна у відліках (24 × 5 хв = 2 год)

# Хронологічний поділ (без перемішування — це часовий ряд!)
SPLIT_TRAIN = 0.70
SPLIT_VAL = 0.15
SPLIT_TEST = 0.15

# Рівні масштабування похибки для досліду E2.
# 0.0 — ідеальний датчик, 1.0 — паспортна похибка, >1 — деградований канал
NOISE_SCALES = [0.0, 0.5, 1.0, 2.0, 3.0, 5.0]

# Кількість незалежних прогонів (різні зерна) для довірчих інтервалів
N_SEEDS = 5
SEED = 42
